# Shared Package

Codigo y contratos que pueden usar frontend y backend.

Mantener este directorio chico. Si algo solo sirve para frontend, queda en `apps/web/src`. Si algo solo sirve para backend, queda en `apps/api/src/shared`.
