# Shared

Codigo y contratos que pueden usar frontend y backend.

Mantener este directorio chico. Si algo solo sirve para frontend, queda en `src/`. Si algo solo sirve para backend, queda en `backend/src/shared`.
